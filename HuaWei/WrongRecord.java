import java.util.*;/** * @author : mengmuzi * create at:  2019-04-03  13:08 * @description: 简单错误记录 */public class WrongRecord {        public static void main(String[] args) {            Map<String,Integer> map = new LinkedHashMap<>();            Scanner scanner = new Scanner(System.in);            while(scanner.hasNext()){                String path = scanner.next() ;                if(path.equals("\\n")){                    break;                }                int line = scanner.nextInt();                int index = path.lastIndexOf("\\");                String sub = index > -1 ? path.substring(index+1): path;//文件名                sub = sub.length() > 16 ? sub.substring(sub.length()-16) : sub;                map.put(sub + " " + line,map.get(sub + " " + line)== null ? 1 : map.get(sub + " " + line) + 1);            }            scanner.close();            //Treemap按照value值进行排序            List<Map.Entry<String,Integer>> list = new LinkedList<>(map.entrySet());            Collections.sort(list, new Comparator<Map.Entry<String, Integer>>() {                @Override                public int compare(Map.Entry<String, Integer> o1, Map.Entry<String, Integer> o2) {                    return o2.getValue().compareTo(o1.getValue());                }            });            Iterator<Map.Entry<String,Integer>> it = list.iterator();            Map.Entry<String,Integer> entry;            int m = 0;            while (m++<8 && it.hasNext()) {                entry = it.next();                System.out.println(entry.getKey() + " " + entry.getValue());            }        }}